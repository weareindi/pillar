{# Anthing you want to appear before the closing body tag #}

{# `wp_footer` is used by Timber to populate Timber powered WordPress themes #}
{{ wp_footer }}

{# Project Scripts #}
<script>
    {# Register Theme Directory Variable #}
    window.themeDir = '{{ theme.link|raw }}';
    window.themeVersion = '{{ version|raw }}';
    window.ajaxUrl = '{{ ajax_url|raw }}';

    {# Inline Cut The Mustard Script #}
    {{ source('_assets/js/script.js', ignore_missing = true) }}

    {# Register service worker #}
    // if (navigator.serviceWorker) {
        // navigator.serviceWorker.register('/sw.js', {
        //     scope: './'
        // });
    // }
</script>

{# Keep svgsprite() but prepopulate icons which may only appear via lazyloaded content. DO NOT STUFF ALL ICONS IN HERE #}
{{ svgsprite([])|raw }}